// Re-export shim. The platform yaku maps now live in the kandora-core schema package (app/db).
// Kept for backwards-compatible `~/api/yaku/platformYakuMaps` imports.
export * from "~/db/yaku/platformYakuMaps";
