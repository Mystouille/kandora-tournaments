// Re-export shim. The league-config type module now lives in the kandora-core schema
// package (app/core). Kept for backwards-compatible `~/services/league-configs/types` imports.
export * from "~/core/types/league-config";
