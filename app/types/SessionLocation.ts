// Re-export shim. Canonical definitions live in the kandora-core schema package (app/db).
// Kept for backwards-compatible `~/types/SessionLocation` imports.
export * from "~/db/types/SessionLocation";
