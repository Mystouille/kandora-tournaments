const http=require('http'),WebSocket=require('ws'); const expr=process.argv.slice(2).join(' '); http.get('http://127.0.0.1:9222/json',r=>{let d='';r.on('data',x=>d+=x);r.on('end',()=>{let t=JSON.parse(d).find(x=>x.title==='Kandora'),w=new WebSocket(t.webSocketDebuggerUrl),id=1;w.on('open',()=>w.send(JSON.stringify({id,method:'Runtime.evaluate',params:{expression:expr,returnByValue:true,awaitPromise:true}})));w.on('message',m=>{let x=JSON.parse(m);if(x.id===id){console.log(JSON.stringify(x.result?.result?.value??x,null,2));w.close()}})})})
